# This file is not used - swhkdrc is now generated by NixOS
#
# The swhkd configuration is generated in features/swhkd.nix using
# values from systems/<host>/variables.nix for shell-agnostic operation.
#
# Configurable variables (in variables.nix under user.hyprland):
#   - terminal: Terminal emulator command
#   - fileManager: File manager command  
#   - webBrowser: Web browser command
#   - menu: Application launcher command (e.g., "rofi -show drun" or "noctalia-shell ipc call launcher toggle")
#
# See features/swhkd.nix for the full keybinding configuration.
